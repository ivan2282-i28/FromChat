{
    "name": "@fromchat/shared",
    "version": "1.0.0",
    "description": "Shared code between FromChat frontend and mobile apps",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "tweetnacl": "^1.0.3"
    },
    "devDependencies": {
        "typescript": "~5.9.2"
    },
    "files": [
        "dist/**/*"
    ]
}
